# Services for Notion Garden Care integration

mark_as_watered:
  name: Mark Plant as Watered
  description: Updates the "Last Watered" date in Notion for a plant or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Tomatoes"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

mark_as_fertilized:
  name: Mark Plant as Fertilized
  description: Updates the "Last Fertilized" date in Notion for a plant or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Rose Bush"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

mark_as_pruned:
  name: Mark Plant as Pruned
  description: Updates the "Last Pruned" date in Notion for a plant or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Apple Tree"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

mark_as_aerated:
  name: Mark Lawn as Aerated
  description: Updates the "Last Aeration" date in Notion for a lawn or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the lawn/plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the lawn (alternative to selecting entity)
      example: "Lawn"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

mark_as_harvested:
  name: Mark Plant as Harvested
  description: Updates the "Last Harvested" date in Notion for a plant or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Tomatoes"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

mark_as_sanded:
  name: Mark Lawn as Sanded
  description: Updates the "Last Sanded" date in Notion for a lawn or all plants in a HA area
  fields:
    entity_id:
      name: Plant
      description: Select the lawn sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the lawn (alternative to selecting entity)
      example: "Lawn"
      selector:
        text:
    area_id:
      name: Area
      description: Update all plants in this HA area (alternative to selecting a single plant)
      selector:
        area:
    date:
      name: Date
      description: Date to set (defaults to today if not specified)
      selector:
        date:

update_plant_property:
  name: Update Plant Property
  description: Updates any property in Notion for a plant. Supports text, number, checkbox, date, and multi-select values.
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Tomatoes"
      selector:
        text:
    property_name:
      name: Property Name
      description: Select the Notion property to update
      required: true
      selector:
        select:
          options:
            # ── Basic Info ──
            - "Type"
            - "Active"
            - "Lifecycle"
            - "Plant Date"
            # ── Plant Characteristics ──
            - "Height"
            - "Growth per Year"
            - "Hardiness Zone"
            # ── Environment ──
            - "Sun Exposure"
            - "Soil Type"
            - "Soil pH"
            # ── Watering ──
            - "Water Interval (days)"
            - "Last Watered"
            - "Water Amount"
            # ── Fertilizing ──
            - "Fertilize Interval (days)"
            - "Last Fertilized"
            - "Fertilizer Type"
            # ── Pruning ──
            - "Prune Months"
            - "Last Pruned"
            - "Prune Instructions"
            # ── Harvest ──
            - "Harvest Months"
            - "Last Harvested"
            - "Harvest Notes"
            # ── Lawn Care ──
            - "Aeration Interval (days)"
            - "Last Aeration"
            - "Sanding Interval (days)"
            - "Last Sanded"
            # ── Companions & Safety ──
            - "Companion Plants"
            - "Bad Companions"
            - "Bee Friendly"
            - "Toxicity"
            - "Winterize"
            # ── Notes ──
            - "Care Instructions"
            - "Special Notes"
            - "Notes"
            - "Additional Information"
    property_value:
      name: Property Value
      description: "The value to set. Type is auto-detected: number for integers/floats, true/false for checkbox, YYYY-MM-DD for dates, list for multi-select, otherwise text"
      example: "7"
      required: true
      selector:
        text:

refresh_database:
  name: Refresh Database
  description: Manually refresh the Notion Garden Care database data

add_plant:
  name: Add Plant (AI)
  description: Add a new plant using AI to automatically fill in care information. Requires a conversation agent to be configured in the integration options.
  fields:
    plant_name:
      name: Plant Name
      description: The name of the plant to add (e.g., "Lavender", "Strawberry", "Mint")
      example: "Lavender"
      required: true
      selector:
        text:

delete_plant:
  name: Delete Plant
  description: Permanently removes a plant from the Notion database (moves the page to trash) and deletes the corresponding sensor.
  fields:
    entity_id:
      name: Plant
      description: Select the plant sensor to delete
      required: true
      selector:
        entity:
          integration: notion_garden_care
          domain: sensor
    page_id:
      name: Page ID
      description: The Notion page ID (alternative to selecting entity)
      example: "1234567890abcdef"
      selector:
        text:
    plant_name:
      name: Plant Name
      description: The name of the plant (alternative to selecting entity)
      example: "Tomatoes"
      selector:
        text:
